 1. Constituer le corpus dans le repertoire data

2. copier analyse.py et process.sh dans le repertoire DPLP
 
 3.Lancer le script process_data.sh (avec commande bash) dans DPLP

4.Rq : les fichies .brackets contiennent les noms de relations : servent à l'evaluation d'après le readme du depots git de DPLP. 
